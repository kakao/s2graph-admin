%h3 CREATE SERVICE
#s2admin-form-layout
  = render 'form'
  = link_to 'Back', services_path

:javascript
  $(document).ready(function() {
    var uuid = generateUUID()
    $("#service_access_token").val(uuid)
    $("#service_access_token_disabled").val(uuid)
  });